      //distinguish Player 1 as X and Player 2 as O
    // if(curPlayer == 1){
    //     playerChar = 'X';
    // }
    // if(curPlayer == 2){
    //     playerChar = 'O';
    // }

  //   while(isGameOver != 0){
  //       printBoard(board, numRows, numCols);

  //       colChoice = getPlay(numCols, numRows, board);
        
  //    //FIXME:
  //       printf("Passed column checking\n");
        // printf("what is colchoice value? %d\n", colChoice);
        
  //       // for (i=0;)
  //       // printf("%d\n", heights);


  //       // if(heights[colChoice] >= numRows){
  //       //     colChoice = getPlay(numCols, numRows, board);
  //       //     continue;
  //       // }
    
  //       i = numRows - heights[colChoice] - 1;
  //       i = numRows - heights[colChoice] - 1;

  //       board[i][colChoice] = playerChar;
        
  //       if(curPlayer == 1){
  //           curPlayer = 2;
  //       }
  //       if(curPlayer == 2){
  //           curPlayer = 1;
  //       }




    // printf("Enter a column between 0 and %d to play in: ", numCols - 1);
    // scanf("%d", &colChoice);
   
 // //FIXME:
 //    printf("scanned choice: %d\n", colChoice);

 //    // playValid(colChoice, numRows, numCols, board);

 //  //fixme: what's col choice?
 //    printf("colchoice: %d\n", colChoice);

    //1 == true 0==false,doesn't work
    //if the play isn't valid, it will return a value 0
    //so that you can prompt the user for input again

    // do{
    //     printf("Enter a column between 0 and %d to play in: ", numCols - 1);
    //     scanf("%d", &colChoice);
    //     playValidity = playValid(colChoice, numRows, numCols, board);
    // } while (playValidity == 0);
// }


    //     playValidity = playValid(colChoice, numRows, numCols, board);

    // // while(playValid(colChoice, numRows, numCols, board) == '0'){
    //     if (playValidity == 0){
    //         printf("didn't work now I'm asking user again!\n");
    //     printf("Enter a column between 0 and %d to play in: ", numCols - 1);
    //     scanf("%d", &colChoice);

        // else{
        //     continue;
        // }
        // // playValidity = playValid(colChoice, numRows, numCols, board);
        // }


